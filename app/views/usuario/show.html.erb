<h1><%= @usuario.nombre + " " + @usuario.apellido %></h1>

<% if usuario_signed_in? and current_usuario.can_follow?(@usuario.id) %>
    <button class="btn btn-info" id="follw_btn" data-friend="<%= @usuario.id %>" ><i class="fa fa-user-plus"></i> Follow</button>
<% end %>